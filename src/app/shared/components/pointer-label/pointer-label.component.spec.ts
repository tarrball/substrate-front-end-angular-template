import { PointerLabelComponent } from './pointer-label.component';

describe('PointerLabelComponent', () => {
    let component: PointerLabelComponent;

    beforeEach(() => {
        component = new PointerLabelComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
