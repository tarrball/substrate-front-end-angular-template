import { UpgradeComponent } from './upgrade.component';

describe('UpgradeComponent', () => {
    let component: UpgradeComponent;

    beforeEach(() => {
        component = new UpgradeComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
