import { TemplateModuleComponent } from './template-module.component';

describe('TemplateModuleComponent', () => {
    let component: TemplateModuleComponent;

    beforeEach(() => {
        component = new TemplateModuleComponent();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
